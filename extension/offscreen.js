chrome.runtime.onMessage.addListener((message, _sender, sendResponse) => { if (message.target !== "offscreen") return; (async () => { if (message.action === "copy") { await copyImage(message.dataUrl); sendResponse({ ok: true }); } if (message.action === "crop") sendResponse({ ok: true, dataUrl: await cropImage(message.dataUrl, message.region) }); })().catch(error => sendResponse({ ok: false, error: error.message })); return true; });
async function copyImage(dataUrl) { const blob = await (await fetch(dataUrl)).blob(); await navigator.clipboard.write([new ClipboardItem({ "image/png": blob })]); }
async function cropImage(dataUrl, region) { const blob=await (await fetch(dataUrl)).blob(),bitmap=await createImageBitmap(blob),scale=region.scale||1,canvas=new OffscreenCanvas(Math.round(region.width*scale),Math.round(region.height*scale)),ctx=canvas.getContext("2d");ctx.drawImage(bitmap,Math.round(region.x*scale),Math.round(region.y*scale),Math.round(region.width*scale),Math.round(region.height*scale),0,0,canvas.width,canvas.height);return blobToDataUrl(await canvas.convertToBlob({type:"image/png"})); }
function blobToDataUrl(blob) { return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=reject;reader.readAsDataURL(blob);}); }
